# -- #vss365 today redirect
vss365today.com {
	header -server
	redir https://vss365today.github.io{uri} permanent
}

www.vss365today.com {
	header -server
	redir https://vss365today.github.io{uri} permanent
}
